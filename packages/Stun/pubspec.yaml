name: stun
description: A complete STUN (Session Traversal Utilities for NAT) protocol implementation for NAT traversal and public IP discovery with IPv4/IPv6 dual-stack support.
version: 1.0.1
repository: https://github.com/elguala9/StunDart
homepage: https://github.com/elguala9/StunDart
resolution: workspace

environment:
  sdk: ^3.9.4

dependencies:
  # Add your dependencies here

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
