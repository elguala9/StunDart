name: stundart_tests
description: Test suite for the StunDart package
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: '^3.9.4'

dependencies:
  stun:
    path: ../Stun

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
